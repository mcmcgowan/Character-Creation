import React from "react";

const CharacterCard = ({
    info, openModal
}) => {
    const {
        playerName, charName, race, className, level, alignment, background, str, dex, con, int, wis, cha
    } = info
 
    return (
        <div>Character Card Placeholder</div>
    )
}

export default CharacterCard;
